import { global, ctx, inputManager } from "../../../global.js"
import { TargetCard } from "../target_card.js";

export class TargetAttackCard extends TargetCard {
    constructor() {
        super(
            "#ff4848"
        )
    }
}