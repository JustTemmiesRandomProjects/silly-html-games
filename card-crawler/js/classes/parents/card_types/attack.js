import { global, ctx, inputManager } from "../../../global.js"
import { SimpleCard } from "../simple_card.js";

export class AttackCard extends SimpleCard {
    constructor() {
        super(
            "#ffb3ba"
        )
    }
}